import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFFE3F2FD);       // bluish body
  static const Color appBar = Color(0xFF4CAF50);           // greenish app bar
  static const Color appBarIcon = Color(0xFF1A237E);       // dark bluish icons
  static const Color primary = Color(0xFF2196F3);          // primary blue
  static const Color accent = Color(0xFF81C784);           // optional accent
  static const Color text = Color(0xFF212121);             // main text
}